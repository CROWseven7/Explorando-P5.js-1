function setup() {
  createCanvas(700, 500);
  background(77);
  }

function draw() {  
  
 stroke ("gray");
  fill ("gray");
  
  //console.Log(mouseIsPressed);
  
  if (mouseIsPressed)  { circle(mouseX,mouseY,30,30);
                        
    }
}